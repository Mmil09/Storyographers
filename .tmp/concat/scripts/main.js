// var app = angular.module('soApp', []);
'use strict';

/*

(function() {

	var setLogoHeight = function() {
		var containerWidth = $('.header-container').width();
		var logoHeight = containerWidth * 0.1342;
		var logoBarHeight = containerWidth * 0.0114;
		var containerHeight = containerWidth * 0.17;
		console.log(logoBarHeight);
		console.log(logoHeight);
		$('.logo').height(logoHeight);
		$('.logo-bar').height(logoBarHeight);
		$('.logo-wrapper').height(containerHeight);
		console.log($('.logo-bar').height());
		console.log($('.logo').height());

	};

	$(window).resize(setLogoHeight);

	$(document).ready(function() {
		setLogoHeight();

		var animationLength = 500;

		$('.logo-center').show('scale', 3000);
		setTimeout(function() {
			$('.logo-left').show("drop", 1000);
		}, 1000);
		setTimeout(function() {
			$('.logo-right').show(animationLength);
		}, 2000);
		setTimeout(function() {
			$('.logo-bar').show(animationLength);
		}, 3000);	
	});
})();

*/